node_modules/
viewer/dist/
# generated from the vault by scripts/build-kips.ts (predev/prebuild): index.json,
# kips/<id>.json, and related.json copied from tools/semantic/related.json.
# Fetched at runtime (not bundled) so the shipped JS stays small.
viewer/public/data/
.DS_Store
*.log
# Obsidian per-user workspace state (keep shared config, ignore local state)
vault/.obsidian/workspace*.json
vault/.obsidian/cache
# Ingestion: raw upstream payload snapshots referenced by ChangeEvent.payloadRef
tools/ingest-cache/
apps/web/dist/
apps/web/.wrangler/
spikes/fluss-flink-compat/homelab/evidence/
apps/web/r2-seed/
apps/web/.e2e/
playwright-report/
playwright-report-development/
test-results/
